# Materialized View

A materialized view is a special trigger that stores the result of a SELECT query on data, as it is inserted, into a target table:

![](https://clickhouse.com/uploads/materialized_view_5a321dc56d.png)
